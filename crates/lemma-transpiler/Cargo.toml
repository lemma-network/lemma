[package]
name = "lemma-transpiler"
version = "0.1.0"
edition = "2021"
description = "Solidity-to-Lem transpiler: parse Solidity source, emit equivalent Lem source"

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
solang-parser = { workspace = true }
# lemma-core added in Batch 2 when mapper first uses LemType::Address ← lemma_core::Address

[dev-dependencies]
serde_json = { workspace = true }
# lemma-lang for round-trip tests: tokenize+parse the emitted Lem source (MF-3 fix)
lemma-lang = { path = "../lemma-lang" }
